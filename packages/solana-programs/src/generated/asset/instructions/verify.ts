/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const VERIFY_DISCRIMINATOR = 11

export function getVerifyDiscriminatorBytes() {
  return getU8Encoder().encode(VERIFY_DISCRIMINATOR)
}

export type VerifyInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountCreator extends string
        ? ReadonlySignerAccount<TAccountCreator> & AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      ...TRemainingAccounts,
    ]
  >

export type VerifyInstructionData = { discriminator: number }

export type VerifyInstructionDataArgs = {}

export function getVerifyInstructionDataEncoder(): FixedSizeEncoder<VerifyInstructionDataArgs> {
  return transformEncoder(getStructEncoder([["discriminator", getU8Encoder()]]), (value) => ({
    ...value,
    discriminator: VERIFY_DISCRIMINATOR,
  }))
}

export function getVerifyInstructionDataDecoder(): FixedSizeDecoder<VerifyInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]])
}

export function getVerifyInstructionDataCodec(): FixedSizeCodec<
  VerifyInstructionDataArgs,
  VerifyInstructionData
> {
  return combineCodec(getVerifyInstructionDataEncoder(), getVerifyInstructionDataDecoder())
}

export type VerifyInput<
  TAccountAsset extends string = string,
  TAccountCreator extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** Creator account to verify */
  creator: TransactionSigner<TAccountCreator>
}

export function getVerifyInstruction<
  TAccountAsset extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: VerifyInput<TAccountAsset, TAccountCreator>,
  config?: { programAddress?: TProgramAddress }
): VerifyInstruction<TProgramAddress, TAccountAsset, TAccountCreator> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [getAccountMeta(accounts.asset), getAccountMeta(accounts.creator)],
    data: getVerifyInstructionDataEncoder().encode({}),
    programAddress,
  } as VerifyInstruction<TProgramAddress, TAccountAsset, TAccountCreator>)
}

export type ParsedVerifyInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** Creator account to verify */
    creator: TAccountMetas[1]
  }
  data: VerifyInstructionData
}

export function parseVerifyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedVerifyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { asset: getNextAccount(), creator: getNextAccount() },
    data: getVerifyInstructionDataDecoder().decode(instruction.data),
  }
}
