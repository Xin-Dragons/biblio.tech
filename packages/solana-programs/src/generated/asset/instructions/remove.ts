/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"
import {
  getExtensionTypeDecoder,
  getExtensionTypeEncoder,
  type ExtensionType,
  type ExtensionTypeArgs,
} from "../types"

export const REMOVE_DISCRIMINATOR = 16

export function getRemoveDiscriminatorBytes() {
  return getU8Encoder().encode(REMOVE_DISCRIMINATOR)
}

export type RemoveInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountGroup extends string ? ReadonlyAccount<TAccountGroup> : TAccountGroup,
      TAccountRecipient extends string ? WritableAccount<TAccountRecipient> : TAccountRecipient,
      ...TRemainingAccounts,
    ]
  >

export type RemoveInstructionData = { discriminator: number; extensionType: ExtensionType }

export type RemoveInstructionDataArgs = { extensionType: ExtensionTypeArgs }

export function getRemoveInstructionDataEncoder(): FixedSizeEncoder<RemoveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["extensionType", getExtensionTypeEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_DISCRIMINATOR })
  )
}

export function getRemoveInstructionDataDecoder(): FixedSizeDecoder<RemoveInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["extensionType", getExtensionTypeDecoder()],
  ])
}

export function getRemoveInstructionDataCodec(): FixedSizeCodec<
  RemoveInstructionDataArgs,
  RemoveInstructionData
> {
  return combineCodec(getRemoveInstructionDataEncoder(), getRemoveInstructionDataDecoder())
}

export type RemoveInput<
  TAccountAsset extends string = string,
  TAccountAuthority extends string = string,
  TAccountGroup extends string = string,
  TAccountRecipient extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** The authority of the asset */
  authority: TransactionSigner<TAccountAuthority>
  /** The asset defining the group, if applicable */
  group?: Address<TAccountGroup>
  /** The account receiving refunded rent */
  recipient: Address<TAccountRecipient>
  extensionType: RemoveInstructionDataArgs["extensionType"]
}

export function getRemoveInstruction<
  TAccountAsset extends string,
  TAccountAuthority extends string,
  TAccountGroup extends string,
  TAccountRecipient extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RemoveInput<TAccountAsset, TAccountAuthority, TAccountGroup, TAccountRecipient>,
  config?: { programAddress?: TProgramAddress }
): RemoveInstruction<
  TProgramAddress,
  TAccountAsset,
  TAccountAuthority,
  TAccountGroup,
  TAccountRecipient
> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    group: { value: input.group ?? null, isWritable: false },
    recipient: { value: input.recipient ?? null, isWritable: true },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.group),
      getAccountMeta(accounts.recipient),
    ],
    data: getRemoveInstructionDataEncoder().encode(args as RemoveInstructionDataArgs),
    programAddress,
  } as RemoveInstruction<
    TProgramAddress,
    TAccountAsset,
    TAccountAuthority,
    TAccountGroup,
    TAccountRecipient
  >)
}

export type ParsedRemoveInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** The authority of the asset */
    authority: TAccountMetas[1]
    /** The asset defining the group, if applicable */
    group?: TAccountMetas[2] | undefined
    /** The account receiving refunded rent */
    recipient: TAccountMetas[3]
  }
  data: RemoveInstructionData
}

export function parseRemoveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRemoveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount()
    return accountMeta.address === ASSET_PROGRAM_PROGRAM_ADDRESS ? undefined : accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      asset: getNextAccount(),
      authority: getNextAccount(),
      group: getNextOptionalAccount(),
      recipient: getNextAccount(),
    },
    data: getRemoveInstructionDataDecoder().decode(instruction.data),
  }
}
