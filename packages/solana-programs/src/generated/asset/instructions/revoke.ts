/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"
import {
  getDelegateInputDecoder,
  getDelegateInputEncoder,
  type DelegateInput,
  type DelegateInputArgs,
} from "../types"

export const REVOKE_DISCRIMINATOR = 6

export function getRevokeDiscriminatorBytes() {
  return getU8Encoder().encode(REVOKE_DISCRIMINATOR)
}

export type RevokeInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >

export type RevokeInstructionData = { discriminator: number; delegateInput: DelegateInput }

export type RevokeInstructionDataArgs = { delegateInput: DelegateInputArgs }

export function getRevokeInstructionDataEncoder(): Encoder<RevokeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["delegateInput", getDelegateInputEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REVOKE_DISCRIMINATOR })
  )
}

export function getRevokeInstructionDataDecoder(): Decoder<RevokeInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["delegateInput", getDelegateInputDecoder()],
  ])
}

export function getRevokeInstructionDataCodec(): Codec<
  RevokeInstructionDataArgs,
  RevokeInstructionData
> {
  return combineCodec(getRevokeInstructionDataEncoder(), getRevokeInstructionDataDecoder())
}

export type RevokeInput<
  TAccountAsset extends string = string,
  TAccountSigner extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** Owner of the asset or current delegate */
  signer: TransactionSigner<TAccountSigner>
  delegateInput: RevokeInstructionDataArgs["delegateInput"]
}

export function getRevokeInstruction<
  TAccountAsset extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RevokeInput<TAccountAsset, TAccountSigner>,
  config?: { programAddress?: TProgramAddress }
): RevokeInstruction<TProgramAddress, TAccountAsset, TAccountSigner> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [getAccountMeta(accounts.asset), getAccountMeta(accounts.signer)],
    data: getRevokeInstructionDataEncoder().encode(args as RevokeInstructionDataArgs),
    programAddress,
  } as RevokeInstruction<TProgramAddress, TAccountAsset, TAccountSigner>)
}

export type ParsedRevokeInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** Owner of the asset or current delegate */
    signer: TAccountMetas[1]
  }
  data: RevokeInstructionData
}

export function parseRevokeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedRevokeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { asset: getNextAccount(), signer: getNextAccount() },
    data: getRevokeInstructionDataDecoder().decode(instruction.data),
  }
}
