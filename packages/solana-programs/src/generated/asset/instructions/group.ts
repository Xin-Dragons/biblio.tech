/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const GROUP_DISCRIMINATOR = 13

export function getGroupDiscriminatorBytes() {
  return getU8Encoder().encode(GROUP_DISCRIMINATOR)
}

export type GroupInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountGroup extends string ? WritableAccount<TAccountGroup> : TAccountGroup,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type GroupInstructionData = { discriminator: number }

export type GroupInstructionDataArgs = {}

export function getGroupInstructionDataEncoder(): FixedSizeEncoder<GroupInstructionDataArgs> {
  return transformEncoder(getStructEncoder([["discriminator", getU8Encoder()]]), (value) => ({
    ...value,
    discriminator: GROUP_DISCRIMINATOR,
  }))
}

export function getGroupInstructionDataDecoder(): FixedSizeDecoder<GroupInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]])
}

export function getGroupInstructionDataCodec(): FixedSizeCodec<
  GroupInstructionDataArgs,
  GroupInstructionData
> {
  return combineCodec(getGroupInstructionDataEncoder(), getGroupInstructionDataDecoder())
}

export type GroupInput<
  TAccountAsset extends string = string,
  TAccountGroup extends string = string,
  TAccountAuthority extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** Asset account of the group */
  group: Address<TAccountGroup>
  /** The authority of the assets */
  authority: TransactionSigner<TAccountAuthority>
}

export function getGroupInstruction<
  TAccountAsset extends string,
  TAccountGroup extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: GroupInput<TAccountAsset, TAccountGroup, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): GroupInstruction<TProgramAddress, TAccountAsset, TAccountGroup, TAccountAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    group: { value: input.group ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.group),
      getAccountMeta(accounts.authority),
    ],
    data: getGroupInstructionDataEncoder().encode({}),
    programAddress,
  } as GroupInstruction<TProgramAddress, TAccountAsset, TAccountGroup, TAccountAuthority>)
}

export type ParsedGroupInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** Asset account of the group */
    group: TAccountMetas[1]
    /** The authority of the assets */
    authority: TAccountMetas[2]
  }
  data: GroupInstructionData
}

export function parseGroupInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedGroupInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { asset: getNextAccount(), group: getNextAccount(), authority: getNextAccount() },
    data: getGroupInstructionDataDecoder().decode(instruction.data),
  }
}
