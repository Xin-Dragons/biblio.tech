/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const HANDOVER_DISCRIMINATOR = 15

export function getHandoverDiscriminatorBytes() {
  return getU8Encoder().encode(HANDOVER_DISCRIMINATOR)
}

export type HandoverInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountNewAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountNewAuthority extends string
        ? ReadonlySignerAccount<TAccountNewAuthority> & AccountSignerMeta<TAccountNewAuthority>
        : TAccountNewAuthority,
      ...TRemainingAccounts,
    ]
  >

export type HandoverInstructionData = { discriminator: number }

export type HandoverInstructionDataArgs = {}

export function getHandoverInstructionDataEncoder(): FixedSizeEncoder<HandoverInstructionDataArgs> {
  return transformEncoder(getStructEncoder([["discriminator", getU8Encoder()]]), (value) => ({
    ...value,
    discriminator: HANDOVER_DISCRIMINATOR,
  }))
}

export function getHandoverInstructionDataDecoder(): FixedSizeDecoder<HandoverInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]])
}

export function getHandoverInstructionDataCodec(): FixedSizeCodec<
  HandoverInstructionDataArgs,
  HandoverInstructionData
> {
  return combineCodec(getHandoverInstructionDataEncoder(), getHandoverInstructionDataDecoder())
}

export type HandoverInput<
  TAccountAsset extends string = string,
  TAccountAuthority extends string = string,
  TAccountNewAuthority extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** The authority of the asset */
  authority: TransactionSigner<TAccountAuthority>
  /** The new authority of the asset */
  newAuthority: TransactionSigner<TAccountNewAuthority>
}

export function getHandoverInstruction<
  TAccountAsset extends string,
  TAccountAuthority extends string,
  TAccountNewAuthority extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: HandoverInput<TAccountAsset, TAccountAuthority, TAccountNewAuthority>,
  config?: { programAddress?: TProgramAddress }
): HandoverInstruction<TProgramAddress, TAccountAsset, TAccountAuthority, TAccountNewAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    newAuthority: { value: input.newAuthority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.newAuthority),
    ],
    data: getHandoverInstructionDataEncoder().encode({}),
    programAddress,
  } as HandoverInstruction<TProgramAddress, TAccountAsset, TAccountAuthority, TAccountNewAuthority>)
}

export type ParsedHandoverInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** The authority of the asset */
    authority: TAccountMetas[1]
    /** The new authority of the asset */
    newAuthority: TAccountMetas[2]
  }
  data: HandoverInstructionData
}

export function parseHandoverInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedHandoverInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      asset: getNextAccount(),
      authority: getNextAccount(),
      newAuthority: getNextAccount(),
    },
    data: getHandoverInstructionDataDecoder().decode(instruction.data),
  }
}
