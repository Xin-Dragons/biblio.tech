/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const CLOSE_DISCRIMINATOR = 0

export function getCloseDiscriminatorBytes() {
  return getU8Encoder().encode(CLOSE_DISCRIMINATOR)
}

export type CloseInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountBuffer extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBuffer extends string
        ? WritableSignerAccount<TAccountBuffer> & AccountSignerMeta<TAccountBuffer>
        : TAccountBuffer,
      TAccountRecipient extends string ? WritableAccount<TAccountRecipient> : TAccountRecipient,
      ...TRemainingAccounts,
    ]
  >

export type CloseInstructionData = { discriminator: number }

export type CloseInstructionDataArgs = {}

export function getCloseInstructionDataEncoder(): FixedSizeEncoder<CloseInstructionDataArgs> {
  return transformEncoder(getStructEncoder([["discriminator", getU8Encoder()]]), (value) => ({
    ...value,
    discriminator: CLOSE_DISCRIMINATOR,
  }))
}

export function getCloseInstructionDataDecoder(): FixedSizeDecoder<CloseInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]])
}

export function getCloseInstructionDataCodec(): FixedSizeCodec<
  CloseInstructionDataArgs,
  CloseInstructionData
> {
  return combineCodec(getCloseInstructionDataEncoder(), getCloseInstructionDataDecoder())
}

export type CloseInput<
  TAccountBuffer extends string = string,
  TAccountRecipient extends string = string,
> = {
  /** The uninitialized buffer account */
  buffer: TransactionSigner<TAccountBuffer>
  /** The account receiving refunded rent */
  recipient: Address<TAccountRecipient>
}

export function getCloseInstruction<
  TAccountBuffer extends string,
  TAccountRecipient extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: CloseInput<TAccountBuffer, TAccountRecipient>,
  config?: { programAddress?: TProgramAddress }
): CloseInstruction<TProgramAddress, TAccountBuffer, TAccountRecipient> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    buffer: { value: input.buffer ?? null, isWritable: true },
    recipient: { value: input.recipient ?? null, isWritable: true },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [getAccountMeta(accounts.buffer), getAccountMeta(accounts.recipient)],
    data: getCloseInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseInstruction<TProgramAddress, TAccountBuffer, TAccountRecipient>)
}

export type ParsedCloseInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** The uninitialized buffer account */
    buffer: TAccountMetas[0]
    /** The account receiving refunded rent */
    recipient: TAccountMetas[1]
  }
  data: CloseInstructionData
}

export function parseCloseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { buffer: getNextAccount(), recipient: getNextAccount() },
    data: getCloseInstructionDataDecoder().decode(instruction.data),
  }
}
