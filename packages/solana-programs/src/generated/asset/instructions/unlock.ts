/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const UNLOCK_DISCRIMINATOR = 8

export function getUnlockDiscriminatorBytes() {
  return getU8Encoder().encode(UNLOCK_DISCRIMINATOR)
}

export type UnlockInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> & AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >

export type UnlockInstructionData = { discriminator: number }

export type UnlockInstructionDataArgs = {}

export function getUnlockInstructionDataEncoder(): FixedSizeEncoder<UnlockInstructionDataArgs> {
  return transformEncoder(getStructEncoder([["discriminator", getU8Encoder()]]), (value) => ({
    ...value,
    discriminator: UNLOCK_DISCRIMINATOR,
  }))
}

export function getUnlockInstructionDataDecoder(): FixedSizeDecoder<UnlockInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]])
}

export function getUnlockInstructionDataCodec(): FixedSizeCodec<
  UnlockInstructionDataArgs,
  UnlockInstructionData
> {
  return combineCodec(getUnlockInstructionDataEncoder(), getUnlockInstructionDataDecoder())
}

export type UnlockInput<
  TAccountAsset extends string = string,
  TAccountSigner extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** Delegate or owner account */
  signer: TransactionSigner<TAccountSigner>
}

export function getUnlockInstruction<
  TAccountAsset extends string,
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UnlockInput<TAccountAsset, TAccountSigner>,
  config?: { programAddress?: TProgramAddress }
): UnlockInstruction<TProgramAddress, TAccountAsset, TAccountSigner> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [getAccountMeta(accounts.asset), getAccountMeta(accounts.signer)],
    data: getUnlockInstructionDataEncoder().encode({}),
    programAddress,
  } as UnlockInstruction<TProgramAddress, TAccountAsset, TAccountSigner>)
}

export type ParsedUnlockInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** Delegate or owner account */
    signer: TAccountMetas[1]
  }
  data: UnlockInstructionData
}

export function parseUnlockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUnlockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { asset: getNextAccount(), signer: getNextAccount() },
    data: getUnlockInstructionDataDecoder().decode(instruction.data),
  }
}
