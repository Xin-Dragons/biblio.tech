/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"
import {
  getDelegateInputDecoder,
  getDelegateInputEncoder,
  type DelegateInput,
  type DelegateInputArgs,
} from "../types"

export const APPROVE_DISCRIMINATOR = 3

export function getApproveDiscriminatorBytes() {
  return getU8Encoder().encode(APPROVE_DISCRIMINATOR)
}

export type ApproveInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountDelegate extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountDelegate extends string ? ReadonlyAccount<TAccountDelegate> : TAccountDelegate,
      ...TRemainingAccounts,
    ]
  >

export type ApproveInstructionData = { discriminator: number; delegateInput: DelegateInput }

export type ApproveInstructionDataArgs = { delegateInput: DelegateInputArgs }

export function getApproveInstructionDataEncoder(): Encoder<ApproveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["delegateInput", getDelegateInputEncoder()],
    ]),
    (value) => ({ ...value, discriminator: APPROVE_DISCRIMINATOR })
  )
}

export function getApproveInstructionDataDecoder(): Decoder<ApproveInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["delegateInput", getDelegateInputDecoder()],
  ])
}

export function getApproveInstructionDataCodec(): Codec<
  ApproveInstructionDataArgs,
  ApproveInstructionData
> {
  return combineCodec(getApproveInstructionDataEncoder(), getApproveInstructionDataDecoder())
}

export type ApproveInput<
  TAccountAsset extends string = string,
  TAccountOwner extends string = string,
  TAccountDelegate extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** The owner of the asset */
  owner: TransactionSigner<TAccountOwner>
  /** The delegate account */
  delegate: Address<TAccountDelegate>
  delegateInput: ApproveInstructionDataArgs["delegateInput"]
}

export function getApproveInstruction<
  TAccountAsset extends string,
  TAccountOwner extends string,
  TAccountDelegate extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ApproveInput<TAccountAsset, TAccountOwner, TAccountDelegate>,
  config?: { programAddress?: TProgramAddress }
): ApproveInstruction<TProgramAddress, TAccountAsset, TAccountOwner, TAccountDelegate> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    delegate: { value: input.delegate ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.delegate),
    ],
    data: getApproveInstructionDataEncoder().encode(args as ApproveInstructionDataArgs),
    programAddress,
  } as ApproveInstruction<TProgramAddress, TAccountAsset, TAccountOwner, TAccountDelegate>)
}

export type ParsedApproveInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** The owner of the asset */
    owner: TAccountMetas[1]
    /** The delegate account */
    delegate: TAccountMetas[2]
  }
  data: ApproveInstructionData
}

export function parseApproveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApproveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { asset: getNextAccount(), owner: getNextAccount(), delegate: getNextAccount() },
    data: getApproveInstructionDataDecoder().decode(instruction.data),
  }
}
