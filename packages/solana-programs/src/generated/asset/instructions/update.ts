/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  getBooleanDecoder,
  getBooleanEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit"
import { ASSET_PROGRAM_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"
import {
  getExtensionInputDecoder,
  getExtensionInputEncoder,
  type ExtensionInput,
  type ExtensionInputArgs,
} from "../types"

export const UPDATE_DISCRIMINATOR = 10

export function getUpdateDiscriminatorBytes() {
  return getU8Encoder().encode(UPDATE_DISCRIMINATOR)
}

export type UpdateInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountAsset extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountBuffer extends string | AccountMeta<string> = string,
  TAccountGroup extends string | AccountMeta<string> = string,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAsset extends string ? WritableAccount<TAccountAsset> : TAccountAsset,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountBuffer extends string ? WritableAccount<TAccountBuffer> : TAccountBuffer,
      TAccountGroup extends string ? ReadonlyAccount<TAccountGroup> : TAccountGroup,
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >

export type UpdateInstructionData = {
  discriminator: number
  name: Option<string>
  mutable: Option<boolean>
  extension: Option<ExtensionInput>
}

export type UpdateInstructionDataArgs = {
  name: OptionOrNullable<string>
  mutable: OptionOrNullable<boolean>
  extension: OptionOrNullable<ExtensionInputArgs>
}

export function getUpdateInstructionDataEncoder(): Encoder<UpdateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["name", getOptionEncoder(addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()))],
      ["mutable", getOptionEncoder(getBooleanEncoder())],
      ["extension", getOptionEncoder(getExtensionInputEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_DISCRIMINATOR })
  )
}

export function getUpdateInstructionDataDecoder(): Decoder<UpdateInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["name", getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()))],
    ["mutable", getOptionDecoder(getBooleanDecoder())],
    ["extension", getOptionDecoder(getExtensionInputDecoder())],
  ])
}

export function getUpdateInstructionDataCodec(): Codec<
  UpdateInstructionDataArgs,
  UpdateInstructionData
> {
  return combineCodec(getUpdateInstructionDataEncoder(), getUpdateInstructionDataDecoder())
}

export type UpdateInput<
  TAccountAsset extends string = string,
  TAccountAuthority extends string = string,
  TAccountBuffer extends string = string,
  TAccountGroup extends string = string,
  TAccountPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Asset account */
  asset: Address<TAccountAsset>
  /** The authority of the asset */
  authority: TransactionSigner<TAccountAuthority>
  /** Extension buffer (uninitialized asset) account */
  buffer?: Address<TAccountBuffer>
  /** The asset defining the group, if applicable */
  group?: Address<TAccountGroup>
  /** The account paying for the storage fees */
  payer?: TransactionSigner<TAccountPayer>
  /** The system program */
  systemProgram?: Address<TAccountSystemProgram>
  name: UpdateInstructionDataArgs["name"]
  mutable: UpdateInstructionDataArgs["mutable"]
  extension: UpdateInstructionDataArgs["extension"]
}

export function getUpdateInstruction<
  TAccountAsset extends string,
  TAccountAuthority extends string,
  TAccountBuffer extends string,
  TAccountGroup extends string,
  TAccountPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
>(
  input: UpdateInput<
    TAccountAsset,
    TAccountAuthority,
    TAccountBuffer,
    TAccountGroup,
    TAccountPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateInstruction<
  TProgramAddress,
  TAccountAsset,
  TAccountAuthority,
  TAccountBuffer,
  TAccountGroup,
  TAccountPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ASSET_PROGRAM_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    asset: { value: input.asset ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    buffer: { value: input.buffer ?? null, isWritable: true },
    group: { value: input.group ?? null, isWritable: false },
    payer: { value: input.payer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.asset),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.buffer),
      getAccountMeta(accounts.group),
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getUpdateInstructionDataEncoder().encode(args as UpdateInstructionDataArgs),
    programAddress,
  } as UpdateInstruction<
    TProgramAddress,
    TAccountAsset,
    TAccountAuthority,
    TAccountBuffer,
    TAccountGroup,
    TAccountPayer,
    TAccountSystemProgram
  >)
}

export type ParsedUpdateInstruction<
  TProgram extends string = typeof ASSET_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    /** Asset account */
    asset: TAccountMetas[0]
    /** The authority of the asset */
    authority: TAccountMetas[1]
    /** Extension buffer (uninitialized asset) account */
    buffer?: TAccountMetas[2] | undefined
    /** The asset defining the group, if applicable */
    group?: TAccountMetas[3] | undefined
    /** The account paying for the storage fees */
    payer?: TAccountMetas[4] | undefined
    /** The system program */
    systemProgram?: TAccountMetas[5] | undefined
  }
  data: UpdateInstructionData
}

export function parseUpdateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount()
    return accountMeta.address === ASSET_PROGRAM_PROGRAM_ADDRESS ? undefined : accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      asset: getNextAccount(),
      authority: getNextAccount(),
      buffer: getNextOptionalAccount(),
      group: getNextOptionalAccount(),
      payer: getNextOptionalAccount(),
      systemProgram: getNextOptionalAccount(),
    },
    data: getUpdateInstructionDataDecoder().decode(instruction.data),
  }
}
