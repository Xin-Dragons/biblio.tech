/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit"

export const COLLECTION_DISCRIMINATOR = new Uint8Array([48, 160, 232, 205, 191, 207, 26, 141])

export function getCollectionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(COLLECTION_DISCRIMINATOR)
}

export type Collection = {
  discriminator: ReadonlyUint8Array
  /** staker this collection belongs to (32) */
  staker: Address
  /** MCC mint of the collection (32) */
  collectionMint: Address
  /** optional creators (4) */
  creators: Array<Address>
  /** Collection custody type (1) */
  custodial: boolean
  /** Merkle root of allowlist (1 + 32) */
  allowList: Option<Address>
  /** pubkey of token emission config (1 + 32) */
  tokenEmission: Option<Address>
  /** pubkey of selection emission config (1 + 32) */
  selectionEmission: Option<Address>
  /** pubkey of points emission config (1 + 32) */
  pointsEmission: Option<Address>
  /** pubkey of distribution emission config (1 + 32) */
  distributionEmission: Option<Address>
  /** items can be staked, rewards accrue (1) */
  isActive: boolean
  /** The max number of NFTs that can be staked (8) */
  maxStakersCount: bigint
  /** The current number of NFTs staked (8) */
  currentStakersCount: bigint
  /** Bump of the Collection PDA (1) */
  bump: number
}

export type CollectionArgs = {
  /** staker this collection belongs to (32) */
  staker: Address
  /** MCC mint of the collection (32) */
  collectionMint: Address
  /** optional creators (4) */
  creators: Array<Address>
  /** Collection custody type (1) */
  custodial: boolean
  /** Merkle root of allowlist (1 + 32) */
  allowList: OptionOrNullable<Address>
  /** pubkey of token emission config (1 + 32) */
  tokenEmission: OptionOrNullable<Address>
  /** pubkey of selection emission config (1 + 32) */
  selectionEmission: OptionOrNullable<Address>
  /** pubkey of points emission config (1 + 32) */
  pointsEmission: OptionOrNullable<Address>
  /** pubkey of distribution emission config (1 + 32) */
  distributionEmission: OptionOrNullable<Address>
  /** items can be staked, rewards accrue (1) */
  isActive: boolean
  /** The max number of NFTs that can be staked (8) */
  maxStakersCount: number | bigint
  /** The current number of NFTs staked (8) */
  currentStakersCount: number | bigint
  /** Bump of the Collection PDA (1) */
  bump: number
}

/** Gets the encoder for {@link CollectionArgs} account data. */
export function getCollectionEncoder(): Encoder<CollectionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["staker", getAddressEncoder()],
      ["collectionMint", getAddressEncoder()],
      ["creators", getArrayEncoder(getAddressEncoder())],
      ["custodial", getBooleanEncoder()],
      ["allowList", getOptionEncoder(getAddressEncoder())],
      ["tokenEmission", getOptionEncoder(getAddressEncoder())],
      ["selectionEmission", getOptionEncoder(getAddressEncoder())],
      ["pointsEmission", getOptionEncoder(getAddressEncoder())],
      ["distributionEmission", getOptionEncoder(getAddressEncoder())],
      ["isActive", getBooleanEncoder()],
      ["maxStakersCount", getU64Encoder()],
      ["currentStakersCount", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: COLLECTION_DISCRIMINATOR })
  )
}

/** Gets the decoder for {@link Collection} account data. */
export function getCollectionDecoder(): Decoder<Collection> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["staker", getAddressDecoder()],
    ["collectionMint", getAddressDecoder()],
    ["creators", getArrayDecoder(getAddressDecoder())],
    ["custodial", getBooleanDecoder()],
    ["allowList", getOptionDecoder(getAddressDecoder())],
    ["tokenEmission", getOptionDecoder(getAddressDecoder())],
    ["selectionEmission", getOptionDecoder(getAddressDecoder())],
    ["pointsEmission", getOptionDecoder(getAddressDecoder())],
    ["distributionEmission", getOptionDecoder(getAddressDecoder())],
    ["isActive", getBooleanDecoder()],
    ["maxStakersCount", getU64Decoder()],
    ["currentStakersCount", getU64Decoder()],
    ["bump", getU8Decoder()],
  ])
}

/** Gets the codec for {@link Collection} account data. */
export function getCollectionCodec(): Codec<CollectionArgs, Collection> {
  return combineCodec(getCollectionEncoder(), getCollectionDecoder())
}

export function decodeCollection<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<Collection, TAddress>
export function decodeCollection<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<Collection, TAddress>
export function decodeCollection<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
): Account<Collection, TAddress> | MaybeAccount<Collection, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getCollectionDecoder())
}

export async function fetchCollection<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<Collection, TAddress>> {
  const maybeAccount = await fetchMaybeCollection(rpc, address, config)
  assertAccountExists(maybeAccount)
  return maybeAccount
}

export async function fetchMaybeCollection<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<Collection, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config)
  return decodeCollection(maybeAccount)
}

export async function fetchAllCollection(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<Collection>[]> {
  const maybeAccounts = await fetchAllMaybeCollection(rpc, addresses, config)
  assertAccountsExist(maybeAccounts)
  return maybeAccounts
}

export async function fetchAllMaybeCollection(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<Collection>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config)
  return maybeAccounts.map((maybeAccount) => decodeCollection(maybeAccount))
}
