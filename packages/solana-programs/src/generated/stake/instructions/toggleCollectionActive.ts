/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { STAKE_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const TOGGLE_COLLECTION_ACTIVE_DISCRIMINATOR = new Uint8Array([
  193, 184, 194, 37, 245, 33, 204, 101,
])

export function getToggleCollectionActiveDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TOGGLE_COLLECTION_ACTIVE_DISCRIMINATOR)
}

export type ToggleCollectionActiveInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountStaker extends string | AccountMeta<string> = string,
  TAccountCollection extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStaker extends string ? ReadonlyAccount<TAccountStaker> : TAccountStaker,
      TAccountCollection extends string ? WritableAccount<TAccountCollection> : TAccountCollection,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type ToggleCollectionActiveInstructionData = {
  discriminator: ReadonlyUint8Array
  active: boolean
}

export type ToggleCollectionActiveInstructionDataArgs = { active: boolean }

export function getToggleCollectionActiveInstructionDataEncoder(): FixedSizeEncoder<ToggleCollectionActiveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["active", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: TOGGLE_COLLECTION_ACTIVE_DISCRIMINATOR })
  )
}

export function getToggleCollectionActiveInstructionDataDecoder(): FixedSizeDecoder<ToggleCollectionActiveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["active", getBooleanDecoder()],
  ])
}

export function getToggleCollectionActiveInstructionDataCodec(): FixedSizeCodec<
  ToggleCollectionActiveInstructionDataArgs,
  ToggleCollectionActiveInstructionData
> {
  return combineCodec(
    getToggleCollectionActiveInstructionDataEncoder(),
    getToggleCollectionActiveInstructionDataDecoder()
  )
}

export type ToggleCollectionActiveInput<
  TAccountStaker extends string = string,
  TAccountCollection extends string = string,
  TAccountAuthority extends string = string,
> = {
  staker: Address<TAccountStaker>
  collection: Address<TAccountCollection>
  authority: TransactionSigner<TAccountAuthority>
  active: ToggleCollectionActiveInstructionDataArgs["active"]
}

export function getToggleCollectionActiveInstruction<
  TAccountStaker extends string,
  TAccountCollection extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAKE_PROGRAM_ADDRESS,
>(
  input: ToggleCollectionActiveInput<TAccountStaker, TAccountCollection, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): ToggleCollectionActiveInstruction<
  TProgramAddress,
  TAccountStaker,
  TAccountCollection,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    staker: { value: input.staker ?? null, isWritable: false },
    collection: { value: input.collection ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.staker),
      getAccountMeta(accounts.collection),
      getAccountMeta(accounts.authority),
    ],
    data: getToggleCollectionActiveInstructionDataEncoder().encode(
      args as ToggleCollectionActiveInstructionDataArgs
    ),
    programAddress,
  } as ToggleCollectionActiveInstruction<
    TProgramAddress,
    TAccountStaker,
    TAccountCollection,
    TAccountAuthority
  >)
}

export type ParsedToggleCollectionActiveInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    staker: TAccountMetas[0]
    collection: TAccountMetas[1]
    authority: TAccountMetas[2]
  }
  data: ToggleCollectionActiveInstructionData
}

export function parseToggleCollectionActiveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedToggleCollectionActiveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      staker: getNextAccount(),
      collection: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getToggleCollectionActiveInstructionDataDecoder().decode(instruction.data),
  }
}
