/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { STAKE_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const EXTEND_EMISSION_DISCRIMINATOR = new Uint8Array([72, 185, 71, 194, 36, 60, 139, 231])

export function getExtendEmissionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXTEND_EMISSION_DISCRIMINATOR)
}

export type ExtendEmissionInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountStaker extends string | AccountMeta<string> = string,
  TAccountCollection extends string | AccountMeta<string> = string,
  TAccountEmission extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStaker extends string ? WritableAccount<TAccountStaker> : TAccountStaker,
      TAccountCollection extends string ? ReadonlyAccount<TAccountCollection> : TAccountCollection,
      TAccountEmission extends string ? WritableAccount<TAccountEmission> : TAccountEmission,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type ExtendEmissionInstructionData = {
  discriminator: ReadonlyUint8Array
  newEndingTime: bigint
}

export type ExtendEmissionInstructionDataArgs = { newEndingTime: number | bigint }

export function getExtendEmissionInstructionDataEncoder(): FixedSizeEncoder<ExtendEmissionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newEndingTime", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: EXTEND_EMISSION_DISCRIMINATOR })
  )
}

export function getExtendEmissionInstructionDataDecoder(): FixedSizeDecoder<ExtendEmissionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newEndingTime", getI64Decoder()],
  ])
}

export function getExtendEmissionInstructionDataCodec(): FixedSizeCodec<
  ExtendEmissionInstructionDataArgs,
  ExtendEmissionInstructionData
> {
  return combineCodec(
    getExtendEmissionInstructionDataEncoder(),
    getExtendEmissionInstructionDataDecoder()
  )
}

export type ExtendEmissionInput<
  TAccountStaker extends string = string,
  TAccountCollection extends string = string,
  TAccountEmission extends string = string,
  TAccountAuthority extends string = string,
> = {
  staker: Address<TAccountStaker>
  collection: Address<TAccountCollection>
  emission: Address<TAccountEmission>
  authority: TransactionSigner<TAccountAuthority>
  newEndingTime: ExtendEmissionInstructionDataArgs["newEndingTime"]
}

export function getExtendEmissionInstruction<
  TAccountStaker extends string,
  TAccountCollection extends string,
  TAccountEmission extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAKE_PROGRAM_ADDRESS,
>(
  input: ExtendEmissionInput<
    TAccountStaker,
    TAccountCollection,
    TAccountEmission,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress }
): ExtendEmissionInstruction<
  TProgramAddress,
  TAccountStaker,
  TAccountCollection,
  TAccountEmission,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    staker: { value: input.staker ?? null, isWritable: true },
    collection: { value: input.collection ?? null, isWritable: false },
    emission: { value: input.emission ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.staker),
      getAccountMeta(accounts.collection),
      getAccountMeta(accounts.emission),
      getAccountMeta(accounts.authority),
    ],
    data: getExtendEmissionInstructionDataEncoder().encode(
      args as ExtendEmissionInstructionDataArgs
    ),
    programAddress,
  } as ExtendEmissionInstruction<
    TProgramAddress,
    TAccountStaker,
    TAccountCollection,
    TAccountEmission,
    TAccountAuthority
  >)
}

export type ParsedExtendEmissionInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    staker: TAccountMetas[0]
    collection: TAccountMetas[1]
    emission: TAccountMetas[2]
    authority: TAccountMetas[3]
  }
  data: ExtendEmissionInstructionData
}

export function parseExtendEmissionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedExtendEmissionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      staker: getNextAccount(),
      collection: getNextAccount(),
      emission: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getExtendEmissionInstructionDataDecoder().decode(instruction.data),
  }
}
