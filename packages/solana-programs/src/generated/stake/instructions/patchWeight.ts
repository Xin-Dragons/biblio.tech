/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit"
import { STAKE_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const PATCH_WEIGHT_DISCRIMINATOR = new Uint8Array([159, 197, 128, 32, 200, 207, 167, 18])

export function getPatchWeightDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PATCH_WEIGHT_DISCRIMINATOR)
}

export type PatchWeightInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountStaker extends string | AccountMeta<string> = string,
  TAccountCollection extends string | AccountMeta<string> = string,
  TAccountEmission extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStaker extends string ? ReadonlyAccount<TAccountStaker> : TAccountStaker,
      TAccountCollection extends string ? ReadonlyAccount<TAccountCollection> : TAccountCollection,
      TAccountEmission extends string ? WritableAccount<TAccountEmission> : TAccountEmission,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type PatchWeightInstructionData = { discriminator: ReadonlyUint8Array; weight: bigint }

export type PatchWeightInstructionDataArgs = { weight: number | bigint }

export function getPatchWeightInstructionDataEncoder(): FixedSizeEncoder<PatchWeightInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["weight", getU128Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PATCH_WEIGHT_DISCRIMINATOR })
  )
}

export function getPatchWeightInstructionDataDecoder(): FixedSizeDecoder<PatchWeightInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["weight", getU128Decoder()],
  ])
}

export function getPatchWeightInstructionDataCodec(): FixedSizeCodec<
  PatchWeightInstructionDataArgs,
  PatchWeightInstructionData
> {
  return combineCodec(
    getPatchWeightInstructionDataEncoder(),
    getPatchWeightInstructionDataDecoder()
  )
}

export type PatchWeightInput<
  TAccountStaker extends string = string,
  TAccountCollection extends string = string,
  TAccountEmission extends string = string,
  TAccountAuthority extends string = string,
> = {
  staker: Address<TAccountStaker>
  collection: Address<TAccountCollection>
  emission: Address<TAccountEmission>
  authority: TransactionSigner<TAccountAuthority>
  weight: PatchWeightInstructionDataArgs["weight"]
}

export function getPatchWeightInstruction<
  TAccountStaker extends string,
  TAccountCollection extends string,
  TAccountEmission extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAKE_PROGRAM_ADDRESS,
>(
  input: PatchWeightInput<TAccountStaker, TAccountCollection, TAccountEmission, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): PatchWeightInstruction<
  TProgramAddress,
  TAccountStaker,
  TAccountCollection,
  TAccountEmission,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    staker: { value: input.staker ?? null, isWritable: false },
    collection: { value: input.collection ?? null, isWritable: false },
    emission: { value: input.emission ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.staker),
      getAccountMeta(accounts.collection),
      getAccountMeta(accounts.emission),
      getAccountMeta(accounts.authority),
    ],
    data: getPatchWeightInstructionDataEncoder().encode(args as PatchWeightInstructionDataArgs),
    programAddress,
  } as PatchWeightInstruction<
    TProgramAddress,
    TAccountStaker,
    TAccountCollection,
    TAccountEmission,
    TAccountAuthority
  >)
}

export type ParsedPatchWeightInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    staker: TAccountMetas[0]
    collection: TAccountMetas[1]
    emission: TAccountMetas[2]
    authority: TAccountMetas[3]
  }
  data: PatchWeightInstructionData
}

export function parsePatchWeightInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPatchWeightInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      staker: getNextAccount(),
      collection: getNextAccount(),
      emission: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getPatchWeightInstructionDataDecoder().decode(instruction.data),
  }
}
