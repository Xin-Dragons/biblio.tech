/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { STAKE_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const TOGGLE_DISTRIBUTION_DISCRIMINATOR = new Uint8Array([
  25, 115, 16, 227, 227, 167, 155, 200,
])

export function getToggleDistributionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TOGGLE_DISTRIBUTION_DISCRIMINATOR)
}

export type ToggleDistributionInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountStaker extends string | AccountMeta<string> = string,
  TAccountDistribution extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStaker extends string ? ReadonlyAccount<TAccountStaker> : TAccountStaker,
      TAccountDistribution extends string
        ? WritableAccount<TAccountDistribution>
        : TAccountDistribution,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type ToggleDistributionInstructionData = {
  discriminator: ReadonlyUint8Array
  active: boolean
}

export type ToggleDistributionInstructionDataArgs = { active: boolean }

export function getToggleDistributionInstructionDataEncoder(): FixedSizeEncoder<ToggleDistributionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["active", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: TOGGLE_DISTRIBUTION_DISCRIMINATOR })
  )
}

export function getToggleDistributionInstructionDataDecoder(): FixedSizeDecoder<ToggleDistributionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["active", getBooleanDecoder()],
  ])
}

export function getToggleDistributionInstructionDataCodec(): FixedSizeCodec<
  ToggleDistributionInstructionDataArgs,
  ToggleDistributionInstructionData
> {
  return combineCodec(
    getToggleDistributionInstructionDataEncoder(),
    getToggleDistributionInstructionDataDecoder()
  )
}

export type ToggleDistributionInput<
  TAccountStaker extends string = string,
  TAccountDistribution extends string = string,
  TAccountAuthority extends string = string,
> = {
  staker: Address<TAccountStaker>
  distribution: Address<TAccountDistribution>
  authority: TransactionSigner<TAccountAuthority>
  active: ToggleDistributionInstructionDataArgs["active"]
}

export function getToggleDistributionInstruction<
  TAccountStaker extends string,
  TAccountDistribution extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAKE_PROGRAM_ADDRESS,
>(
  input: ToggleDistributionInput<TAccountStaker, TAccountDistribution, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): ToggleDistributionInstruction<
  TProgramAddress,
  TAccountStaker,
  TAccountDistribution,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    staker: { value: input.staker ?? null, isWritable: false },
    distribution: { value: input.distribution ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.staker),
      getAccountMeta(accounts.distribution),
      getAccountMeta(accounts.authority),
    ],
    data: getToggleDistributionInstructionDataEncoder().encode(
      args as ToggleDistributionInstructionDataArgs
    ),
    programAddress,
  } as ToggleDistributionInstruction<
    TProgramAddress,
    TAccountStaker,
    TAccountDistribution,
    TAccountAuthority
  >)
}

export type ParsedToggleDistributionInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    staker: TAccountMetas[0]
    distribution: TAccountMetas[1]
    authority: TAccountMetas[2]
  }
  data: ToggleDistributionInstructionData
}

export function parseToggleDistributionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedToggleDistributionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: {
      staker: getNextAccount(),
      distribution: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getToggleDistributionInstructionDataDecoder().decode(instruction.data),
  }
}
