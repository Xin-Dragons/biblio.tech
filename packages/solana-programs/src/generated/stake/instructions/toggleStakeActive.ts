/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit"
import { STAKE_PROGRAM_ADDRESS } from "../programs"
import { getAccountMetaFactory, type ResolvedAccount } from "../shared"

export const TOGGLE_STAKE_ACTIVE_DISCRIMINATOR = new Uint8Array([
  168, 53, 116, 160, 59, 55, 113, 147,
])

export function getToggleStakeActiveDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TOGGLE_STAKE_ACTIVE_DISCRIMINATOR)
}

export type ToggleStakeActiveInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountStaker extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountStaker extends string ? WritableAccount<TAccountStaker> : TAccountStaker,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >

export type ToggleStakeActiveInstructionData = {
  discriminator: ReadonlyUint8Array
  isActive: boolean
}

export type ToggleStakeActiveInstructionDataArgs = { isActive: boolean }

export function getToggleStakeActiveInstructionDataEncoder(): FixedSizeEncoder<ToggleStakeActiveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["isActive", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: TOGGLE_STAKE_ACTIVE_DISCRIMINATOR })
  )
}

export function getToggleStakeActiveInstructionDataDecoder(): FixedSizeDecoder<ToggleStakeActiveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["isActive", getBooleanDecoder()],
  ])
}

export function getToggleStakeActiveInstructionDataCodec(): FixedSizeCodec<
  ToggleStakeActiveInstructionDataArgs,
  ToggleStakeActiveInstructionData
> {
  return combineCodec(
    getToggleStakeActiveInstructionDataEncoder(),
    getToggleStakeActiveInstructionDataDecoder()
  )
}

export type ToggleStakeActiveInput<
  TAccountStaker extends string = string,
  TAccountAuthority extends string = string,
> = {
  staker: Address<TAccountStaker>
  authority: TransactionSigner<TAccountAuthority>
  isActive: ToggleStakeActiveInstructionDataArgs["isActive"]
}

export function getToggleStakeActiveInstruction<
  TAccountStaker extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof STAKE_PROGRAM_ADDRESS,
>(
  input: ToggleStakeActiveInput<TAccountStaker, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress }
): ToggleStakeActiveInstruction<TProgramAddress, TAccountStaker, TAccountAuthority> {
  // Program address.
  const programAddress = config?.programAddress ?? STAKE_PROGRAM_ADDRESS

  // Original accounts.
  const originalAccounts = {
    staker: { value: input.staker ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  }
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedAccount>

  // Original args.
  const args = { ...input }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId")
  return Object.freeze({
    accounts: [getAccountMeta(accounts.staker), getAccountMeta(accounts.authority)],
    data: getToggleStakeActiveInstructionDataEncoder().encode(
      args as ToggleStakeActiveInstructionDataArgs
    ),
    programAddress,
  } as ToggleStakeActiveInstruction<TProgramAddress, TAccountStaker, TAccountAuthority>)
}

export type ParsedToggleStakeActiveInstruction<
  TProgram extends string = typeof STAKE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>
  accounts: {
    staker: TAccountMetas[0]
    authority: TAccountMetas[1]
  }
  data: ToggleStakeActiveInstructionData
}

export function parseToggleStakeActiveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedToggleStakeActiveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts")
  }
  let accountIndex = 0
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!
    accountIndex += 1
    return accountMeta
  }
  return {
    programAddress: instruction.programAddress,
    accounts: { staker: getNextAccount(), authority: getNextAccount() },
    data: getToggleStakeActiveInstructionDataDecoder().decode(instruction.data),
  }
}
